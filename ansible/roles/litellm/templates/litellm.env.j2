# LiteLLM environment variables
# Get API keys from VaultWardan:
# bw get item ai-cluster/services/anthropic --session "$BW_SESSION" | jq -r '.fields[] | select(.name=="ANTHROPIC_API_KEY") | .value'
# bw get item ai-cluster/services/gemini --session "$BW_SESSION" | jq -r '.fields[] | select(.name=="GEMINI_API_KEY") | .value'

LITELLM_MASTER_KEY={{ litellm_master_key }}

# Cloud API Keys (from VaultWardan)
ANTHROPIC_API_KEY={{ vault_anthropic_api_key | default('') }}
GEMINI_API_KEY={{ vault_gemini_api_key | default('') }}
