---
rule: session-persistence
description: Named session pattern — session name stored for resume across sessions
---

# Session Persistence

Session name persistence enables resume across sessions.
The active session state is tracked in `.opencode/session-name`.
