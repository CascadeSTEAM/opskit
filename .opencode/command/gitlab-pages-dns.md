---
description: GitLab Pages custom-domain verification and DNS record setup for an environment's static site, plus the domain-cutover pattern. Use for: /gitlab-pages-dns, gitlab pages, custom domain, pages domain, alias record, domain cutover, dns provider.
---

Call the skill tool to load the "gitlab-pages-dns" skill, then follow its instructions exactly.

$ARGUMENTS
