---
description: Git commit format, atomic commit rules, and enforcement hooks. Use for: /git, git, commit, push, branch, merge.
---

Call the skill tool to load the "git" skill, then follow its instructions exactly.

$ARGUMENTS
