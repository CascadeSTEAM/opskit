---
description: Rapid-cycle dogfooding — run the real thing against real data, fail, fix, retry until MVP; escalate to a full dev cycle only on a true blocker. Use for: /dogfood-cycle, dogfood, dogfooding, try it, rapid cycle, mvp, iterate, smoke test, does it actually work.
---

Call the skill tool to load the "dogfood-cycle" skill, then follow its instructions exactly.

$ARGUMENTS
