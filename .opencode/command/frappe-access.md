---
description: Routing rule for running code/queries against a Frappe/ERPNext site — HTTP/API (Path A) by default, bin/frappe-exec.py (Path B) only when the API is unavailable or the operation genuinely needs admin.. Use for: /frappe-access, frappe, erpnext, bench, frappe-exec, docker exec, bench console.
---

Call the skill tool to load the "frappe-access" skill, then follow its instructions exactly.

$ARGUMENTS
