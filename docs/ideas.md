# Idea Ledger

Raw enhancement/tooling ideas land here first, as a single ledger
row — not as an individual GitHub issue per idea. This keeps early,
overlapping ideas cheap to capture and consolidatable before they
become tracked work (Development Principle 1: never lose an idea —
but a ticket flood isn't the win condition either).

**Capture vs. triage.** Anyone (an agent or a human) can *capture* an
idea any time via `bin/idea.py add` — no judgment call required, no
board noise. *Triage* — clustering overlapping rows, deciding what's
worth a GitHub issue, and filing it — is a separate, deliberate pass,
done by the `idea-triage` skill
(`.opencode/skills/idea-triage/SKILL.md`). A row only gets a GH# once
triage has actually filed (or matched it to) an issue.

Manage this table with `bin/idea.py` (`add`, `list`, `search`,
`mark`) rather than hand-editing — it keeps pipe-escaping and row
numbering consistent. Run `bin/idea.py --help` for full usage.

| Date | Desire (1-5) | Title | Description | Status | GH# |
|------|--------------|-------|-------------|--------|-----|
| 2026-07-20 | 3 | opskit init: refuse case-insensitive duplicate env names | init scaffolded an uppercase twin of an existing environment; it should detect case-insensitive collisions and refuse (or offer to adopt the existing env) | accepted | 23 |
| 2026-07-20 | 4 | flag inventory hosts missing device YAMLs | generic check: every host in an environment inventory should have a datasets/devices YAML; scan or a lint command should report gaps | accepted | 24 |
| 2026-07-20 | 3 | run opskit lint in CI e2e job | the e2e-pipeline CI job scaffolds a fake env and scans a fixture; running opskit lint against it (and against environments/example) would catch inventory/device-YAML drift on every PR | accepted | 29 |
| 2026-07-21 | 4 | opskit scan: auto-elevate or warn when local-subnet scan runs unprivileged | on a local LAN, nmap needs root for ARP; without it the scan writes device YAMLs with empty MACs and no vendor. cmd_scan should detect a local subnet + non-root and either re-exec with sudo or emit a clear warning (nmap_runner already has use_sudo). Found during a live LAN scan. | accepted | 141 |
| 2026-07-21 | 3 | opskit scan: resolve hostnames from DNS/DHCP, not just reverse-PTR | scan named only ~14% of hosts on a live LAN; the authoritative names came from the Technitium DHCP lease table + Proxmox CT map. scan could optionally query an env-configured DNS/DHCP source (or PVE API) to name hosts instead of leaving host-<ip> stubs. | consolidated | 145 |
| 2026-07-22 | 4 | System baseline capture skill | Codify the manual process of capturing a known-good system state for comparison during troubleshooting. Should capture: OS/kernel, GPU/drivers, display config (KScreen), key packages, services, network config. Output a device YAML baseline file that can be used to restage from scratch. Surfaced during a hands-on display-config debug session on a workstation. | accepted | 37 |
| 2026-07-22 | 4 | Guard branch names against client tokens | publication-guard.sh checks staged content + commit messages but NOT branch names; the AGENTS.md client-data rule explicitly forbids client-identifying branch names. Add a pre-push hook (and/or CI check) rejecting branch names that contain any .client-tokens entry. Surfaced when a client-named feature branch was found already pushed to the public origin. | accepted | 118 |
| 2026-07-23 | 3 | Weak-signal access-list pattern for CAPsMAN APs | After an AP outage, IoT clients fail over to a distant AP and stick there at unusable signal. Codify a CAPsMAN access-list (reject below ~-85 dBm) as a reusable Ansible-managed pattern so sticky clients re-home automatically. | new |  |
| 2026-07-23 | 3 | Detect duplicate DHCP client hostnames; manage reservations | Devices sharing a client hostname make the DHCP-updated DNS A record flap between them; hostname-less devices get no record. Teach scan/tooling to detect collisions and generate DHCP reservations with unique hostnames. | consolidated | 145 |
| 2026-07-23 | 2 | Fix stale endsession skill (missing end-session script) | skills/endsession instructs 'npm run session:end' via scripts/end-session.ts, but neither package.json nor scripts/ exists in this repo. Port the script or rewrite the skill to match the manual AGENTS.md shutdown procedure. | accepted | 130 |
| 2026-07-25 | 4 | ACTIVE_ENV race between concurrent sessions | Two agent sessions sharing one clone fight over .env ACTIVE_ENV (observed: ticket opened with wrong env prefix after another session flipped envs mid-task). Consider per-session env override (env var beats .env), a lock, or warning when .env mtime changes mid-session. | accepted | 126 |
| 2026-07-27 | 2 | run skill: recipe for screenshotting static/browser-driven HTML pages | The 'run' skill has no built-in pattern for driving/screenshotting a plain static HTML file (no dev server, no framework) — had to install playwright locally and use channel:'chrome' to reuse the system google-chrome binary since Playwright's bundled browser build wasn't downloaded. Worth adding a short recipe/example for this shape alongside cli/server/tui/electron/playwright. | new |  |
| 2026-07-27 | 2 | Buzz booking dashboard has no way back to the main Desk | Buzz's customer-facing event-booking SPA (mounted at /dashboard) has no navigation link back to the Frappe Desk once a staff member ends up there (e.g. via an event/ticket link). Not urgent, but a real UX gap surfaced while diagnosing a live helpdesk ticket. | new |  |
| 2026-07-30 | 4 | Client helpdesk TLS cert expired - ticket creation blocked | An env helpdesk endpoint fails TLS handshake (expired proxy cert); open-ticket.sh cannot record real tickets, sessions fall back to --local. Root cause and fix path documented in that env private layer (session note 2026-07-30). Also: that env vault CF token is stale and needs rotation. | accepted | 151 |
| 2026-07-31 | 3 | Rename Frappe site to its canonical vhost to drop reverse-proxy Host rewrite | A Frappe/ERPNext site is still named after the vhost it was originally provisioned under, while the canonical public vhost is now a different name. The edge proxy compensates with a per-vhost 'header_up Host <old-site-name>' rewrite, because Frappe routes requests by Host header. Behaviour is correct, but the mismatch is permanent tech debt: the rewrite can never be removed, the old name stays load-bearing in configs/backups/bench commands, and operators querying the site must know the internal name differs from the URL (this already caused a mis-framed diagnosis in one session). Fix: bench rename-site to the canonical name, update compose/site config, backup jobs and proxy vhosts, then delete the Host-rewrite directive. Needs a maintenance window and a verified backup first. | new |  |
| 2026-07-31 | 4 | Guard: tests must not depend on presence/absence of gitignored local config | A merged change passed CI and failed locally on the byte-identical commit: a module read a gitignored local config file at import time, and the test suite inherited it. CI never has that file, so the suite was permanently green while any developer holding real config saw 41 failures. Fixed case-by-case, but the class of bug is repeatable across any tool reading a *.local.json / .env / environments overlay at import. Worth a CI job (or pytest fixture convention) that runs the suite a second time with representative local-config files present, so environment-coupled tests fail loudly in CI instead of only on developer machines. Related: any test that exec_module's a real module inherits that module's import-time side effects. | accepted | 122 |
| 2026-07-31 | 4 | Lint: quote every value interpolated into an ssh command line | A code review caught arbitrary remote command execution in a new tool: ssh appends its trailing arguments into a SINGLE string that is parsed by a shell on the remote host, so passing values as separate argv elements gives no protection whatsoever. A crafted container name turned into arbitrary execution on the target. Fixed with shlex.quote per token, but the repo builds ssh command lines in several scripts and nothing checks this. Worth a lint rule or shellcheck-style guard that flags unquoted interpolation into ssh/rsync/scp invocations, plus a short note in the relevant skill, since the mistake looks safe to reviewers who assume argv separation is honoured end to end. | accepted | 124 |
| 2026-08-02 | 4 | Guard: detect runtime MCP config pointing outside the repo | The agent runtime launched older duplicate copies of two MCP servers from a sibling repo for weeks; merged tool surface was never reachable and nothing could notice. Add a check (opskit check / CI-adjacent) that reads the runtime MCP config and flags any entry whose launch target is not this repo's bin/mcp-run.sh when an equivalent server exists in mcp/. Same class as the branch-name publication gap: the guard exists for content but not for wiring. | consolidated | 112 |
| 2026-08-02 | 4 | Retire duplicated MCP server copies once the in-repo launcher is proven | Two servers still have older copies living outside the repo, wired in parallel with the new launcher. Once the new path is exercised on real work, delete the outside copies and their per-server wrapper scripts so there is exactly one implementation per server. Leaving both indefinitely reintroduces the drift the launcher was built to end. | accepted | 146 |
| 2026-08-02 | 4 | Flag environment directories with no remote in .env-remotes | An environment present locally but absent from the .env-remotes map exists on exactly one machine and is silently lost in a rebuild or workstation migration. install.sh counts environments but does not cross-check them against the remote map. Add that comparison to opskit check / install.sh preflight. | accepted | 116 |
| 2026-08-02 | 3 | Verify vault item notes against actually-granted permissions | A service-account vault item documented a permission posture that live probing contradicted (notes said roles were not granted; reads returned 200). Credential documentation drifts from reality silently and then gets trusted. A small probe tool that checks what an account can actually do, and reports drift from its documented scope, would catch this. | new |  |
| 2026-08-03 | 4 | Codify VPN peer lifecycle: create, inventory, hand-off, revoke | No process exists for managing WireGuard access. Zero playbooks/roles/skills/docs reference peer management; the private layer records subnets and a maturity:1 device stub only. Symptoms found 2026-08-03 while trying to add a user: dashboard admin password drifted from the vault (vault entry unchanged since 06-16, rejected by the live dashboard), two duplicate vault items for one credential, no inventory of who has access, no revocation path. Per Development Principles #2 this is application record state (WGDashboard API) so the vehicle is an MCP tool or codified CLI, NOT Ansible. Must cover create -> client config -> vault naming scheme -> peer inventory -> secure hand-off -> revoke. | consolidated | 103 |
| 2026-08-03 | 4 | opskit lint ignores env.yml source_of_truth.format and always looks for .yml device files | bin/opskit lint reports '0/18 inventory hosts have device YAML' for an environment whose devices are stored as .md with YAML frontmatter — the format that env.yml explicitly declares via source_of_truth.format: md. So the device-coverage gate has never passed for that env and its output is pure noise, which trains operators to ignore it. Either honour source_of_truth.format, accept both extensions, or converge the layer on one format. Same class as #83: a guard whose own correctness nothing checks. Found 2026-08-03 while filling in a device record. | accepted | 114 |
| 2026-08-03 | 3 | Migrate ansible_env/ansible_hostname to ansible_facts before the injected-vars removal | ansible-core 2.21 emits a deprecation on every workstation playbook run: 'Use ansible_facts["fact_name"] (no ansible_ prefix) instead.' Both workstation playbooks use ansible_env.HOME and ansible_hostname, as do others in ansible/. Currently a warning; becomes a hard failure when injected ansible_* vars are removed. Repo-wide sweep, ansible-lint does not flag it. Noticed while fixing #84. | accepted | 139 |
| 2026-08-03 | 4 | mcp-run.sh --check reports BW_SESSION 'set' when the vault is actually locked | The check only tests that BW_SESSION is non-empty. A stale token from an auto-locked vault passes the check, then every secret resolution fails at launch — precisely the silent MCP startup failure the script exists to prevent. Observed 2026-08-03: bw status returned {"status":"locked"} while --check reported BW_SESSION OK, and bw get item hung on a hidden master-password prompt rather than erroring. Fix: run a cheap authenticated probe (bw status, assert status == unlocked) instead of a non-empty test, and surface 'locked' distinctly from 'not set'. Also worth passing </dev/null to bw calls so a locked vault errors instead of waiting on stdin. | accepted | 112 |
| 2026-08-04 | 3 | scan.py: offer opt-in enrolment when a Proxmox node is discovered | #86 wired the Proxmox MCP launcher and added an install.sh preflight that reports environments whose device dataset has a Proxmox node but no launcher config. The remaining half of that issue's scope is enrolment at discovery time: when bin/scan.py finds a host that looks like a Proxmox node, offer to add the tenants entry — at the operator's option, never automatically, since minting or storing an API token is a live change. Needs a general mechanism (scan currently enriches device YAML only), so it is its own change rather than part of #86. | new |  |
| 2026-08-04 | 4 | The running lifecycle-processor service executes a sibling repo's copy, not this one | systemctl --user shows lifecycle-processor active with ExecStart pointing at a SIBLING repo's checkout and venv (scripts/lifecycle-processor.py there), while this repo ships bin/lifecycle-processor.py. Same wrong-copy class as #80/#81 for MCP servers: the code here has never been what runs. #95 fixed the playbook that would install the correct unit, and its --check --diff shows exactly the cutover, but applying it changes a live service so it is the owner's call (add-alongside precedent). Decide: cut over to this repo's copy, or retire this repo's copy and the playbook. Until then two divergent copies exist with only one running. | accepted | 143 |
| 2026-08-04 | 4 | Device records lack os_version, which blocks matching a lab VM to production | Device datasets record 'os: RouterOS' with no os_version for real devices (only the example device has one). That blocked choosing a RouterOS CHR image matching the production router for #94 testing — the version had to be asked for rather than looked up. os_version matters for any lab-mirrors-production work, for CVE/upgrade tracking, and for the security-auditor member. bin/scan.py and the mikrotik/linux subagents should populate it, and opskit lint could warn when an active device has no os_version. | accepted | 105 |
| 2026-08-04 | 4 | A vault-documented RouterOS playbook does not exist in the repo | A vault item for a scoped read-only RouterOS API user records its provenance as 'Created via ansible/playbooks/mikrotik-provision-mcp-readonly-user.yml'. That playbook is not in the repo and not anywhere on the filesystem — a filesystem-wide find returns nothing, and git log finds no commit that ever added it. So the least-privilege API user that mikromcp is supposed to use has no rebuild path: recreating that router from zero recreates it without the account, and the only record of how it was made is a sentence in a vault note. Exactly the failure mode .opencode/rules/iac-required.md exists to prevent. Either the playbook was never committed or it was removed; either way it needs to be written and committed, then referenced from the vault item. Blocks doing least-privilege properly on the other routers (see #105). | accepted | 105 |
| 2026-08-04 | 3 | Device datasets are never validated against device.schema.json | schemas/device.schema.json marks name/status/owner/maturity as required and constrains 'role' to an enum, but nothing validates the actual records. In one environment, 0 of 16 device files carry the required 'owner' field, and 'role' values outside the enum are in use (e.g. 'lxc' where the enum offers 'container'). tests/test_schemas.py validates the schema files themselves, not the datasets. Consequence: any consumer written against the schema — the SoT adapter interface in schemas/sot-adapter-interface.md, opskit lint, generators that read datasets — cannot rely on required fields being present, which pushes every consumer into defensive guesswork. Wants a lint/test pass that validates every environments/*/datasets/devices/*.md front-matter against the schema, reporting rather than failing at first so the existing drift can be worked down. Surfaced while adding os_version to a device record for ledger row 28. | accepted | 114 |
| 2026-08-04 | 4 | No shell path to invoke mikromcp tools — only serve/doctor | mikromcp's CLI exposes serve, doctor, init and update only; the ~60 RouterOS tools are reachable exclusively over MCP stdio. So any RouterOS read from a shell or script context — a playbook pre-check, a dataset generator, a CI probe, an agent whose MCP namespace is not loaded — has no sanctioned path, which pushes people toward raw REST/SSH that AGENTS.md forbids. Worked around this session with a throwaway 60-line JSON-RPC stdio client to read system status and user groups, used four times in one session, which by dev principle 2 is already past the codify threshold. Wants a small committed tool (bin/mikromcp-call.sh or an mcp/ wrapper) that initializes the session, calls one tool by name with JSON args, and prints structuredContent — reusing bin/mcp-run.sh for vault-resolved credentials so it works the same way the agent runtime does. Would also unblock generating routers.yaml from device datasets, which #105 lists in its definition of done. | accepted | 110 |
| 2026-08-04 | 4 | Onboarding gap: a factory-default device cannot be probed via the sanctioned path | A device found on an environment's network was running the managed OS but carried no os/os_version/ip_address in its record, so an in-service device read as a discovery stub and stayed invisible to tooling that selects on those fields. Three problems recur together in this shape and deserve a general answer: no credential that authenticates (candidate vault items returning 401, including one whose recorded address no longer answers), a dynamic address so the device cannot be pinned by IP, and no scoped API user. It also exposed a real tooling gap worth fixing generically: the RouterOS MCP server treats an EMPTY password as a MISSING credential and refuses to attempt the call, so a factory-default device cannot be probed through the sanctioned path at all - exactly the case you most need when onboarding a new or reset unit. The generic gap belongs here; the specific device is tracked privately in its environment repo per docs/client-data-policy.md. | accepted | env-<name>#1 |
| 2026-08-04 | 4 | Environments should declare a schema and version so upgrade/re-fit need is visible | env.yml has no notion of which version of the opskit environment contract it was written against. Consequence: when the shape of an environment layer changes (a new required key, a renamed section, a dataset field becoming mandatory, a new directory like context/ or lifecycle/), there is no way to tell which environments are behind, so drift is discovered by something breaking rather than by inspection. Wants two things in env.yml: a schema identifier and a version (e.g. 'schema: opskit-env' + 'schema_version: 3'), validated against schemas/env.schema.json, with a tool that reports each environment's version against the current contract and names what a lagging one is missing. Same reasoning applies per-device: schemas/device.schema.json is versionless today, so a record written a year ago is indistinguishable from a current one. This is the environment-level analogue of tracking os_version per device — you cannot plan an upgrade for something whose current version you do not record. Raised by the operator 2026-08-04 while closing #105; relates to ledger row 30 (datasets are never validated against their schema). | accepted | 114 |
| 2026-08-04 | 3 | open-ticket.sh has no --help and treats it as a ticket subject | bin/open-ticket.sh takes its first argument as the ticket subject unconditionally, so 'bin/open-ticket.sh --help' attempts to CREATE a ticket titled '--help' against the live client helpdesk. Observed 2026-08-04: it only failed because helpdesk credentials were not exported at the time, and the error text was about auth rather than about the nonsense subject. With credentials loaded it would have filed a junk ticket on a client's helpdesk. A flag that performs a live action instead of describing one is a trap, and every other script in bin/ responds to --help or --check. Fix: recognise --help/-h before any side effect, print usage, exit 0; and reject subjects that begin with '-' unless passed after an explicit '--' separator. Cheap, and it removes a footgun from the one tool whose side effect lands on someone else's system. | accepted | 120 |
| 2026-08-04 | 4 | The Proxmox MCP server has never been able to start — TLS config rejected at load | Every tenant in the Proxmox MCP tenants config sets verify_ssl: false, and the upstream server refuses to load that unless dev_mode: true is also set — it raises 'Insecure TLS configuration blocked' during config validation and exits before serving anything. So the Proxmox MCP tools have never been available in any agent session; the failure is silent, presenting as the tools simply being absent, which reads as an agent declining to help. Found 2026-08-04 by bin/mcp-call.py, which surfaces the server's stderr instead of swallowing it. Do NOT fix this by setting dev_mode: true — two of the three tenants are client hypervisors, and unverified TLS to a hypervisor API while a flag claims it is fine is the 'looks secure and is not' pattern. The right fix is verify_ssl: true with each node's PVE CA pinned (PVE ships one at /etc/pve/pve-root-ca.pem), which is the same honest-verification decision taken for RouterOS in #106. Also worth an install.sh/mcp-run.sh --check probe that actually starts each server and reports a non-serving one, since --check today validates the launch path but not that the server survives config load. | consolidated | 112 |
| 2026-08-04 | 4 | CHR lab VMs need config pre-seeded via autorun.scr, not scripted serial | Building the RouterOS CHR lab VM for #94 showed that a fresh CHR cannot be configured over a scripted serial console with any reliability. First boot forces an interactive password change, and the console echoes each keystroke as asterisks while re-prompting on mismatch, so a script that sends commands on timers ends up typing them into the password field — observed as a loop of 'New passwords do not match!' followed by the subsequent config commands being consumed as password attempts. Waiting for the prompt pattern instead of sleeping did not help either; the change prompt does not present the normal prompt at all. The deterministic path is MikroTik's own: mount the raw image before first boot and drop an autorun.scr on its primary partition with the password, service enables and identity, so the VM comes up already configured and never shows the interactive prompt. Worth codifying as a playbook or a bin/ tool, because a disposable lab router is going to be wanted repeatedly — for #94, for #106's TLS work, for testing the read-only-user playbook, and for any future RouterOS change that should not be rehearsed on production. Should take a target version so the lab can match whichever production device is in question. | new |  |
| 2026-08-05 | 4 | One helpdesk tenant has no vault credentials, so its tickets cannot be filed | The helpdesk MCP server supports multiple tenants but only one has API credentials in the vault map; the other returns 'ERPNEXT_API_KEY_<TENANT>/SECRET not set' on every call. Consequence surfaced 2026-08-05: work that belongs on that environment's helpdesk had to be filed in its private git repo instead, because the sanctioned ticket path was unavailable. That is a workable fallback but it splits the record across two systems, and the client-facing half never reaches the client. Needs a service account on that tenant with the helpdesk Agent role, its key/secret stored in the vault, and the pair added to the vault map under erpnext. Also worth an install.sh/preflight report naming tenants that are configured in the server but have no resolvable credential, since today the gap is only discovered by attempting a write. | new |  |
| 2026-08-05 | 3 | No single view of work across the public repo and per-environment repos | Work now lives in three places: public opskit issues (engineering), per-environment private repo issues (devices and infrastructure state, per docs/client-data-policy.md), and per-environment helpdesks (client-facing). That routing is correct for data isolation but there is no combined view, so per-environment work is invisible unless someone remembers to open that specific repo. Asked directly by the operator on 2026-08-05 - 'if it is on the environment repo, will we still see it in our issues list?' - and the honest answer today is no, not without looking there. Wants a small aggregator: a bin/ tool or gh alias that lists open issues across the public repo plus every environment repo named in .env-remotes, and optionally open helpdesk tickets per tenant, printing them as one list tagged by origin. Read-only, so it can run at session start alongside the existing preflight. | new |  |
| 2026-08-05 | 4 | Verify AGENTS.md and CLAUDE.md against the repo they describe, and track hand-edits to them | AGENTS.md is the highest-leverage file in the repo: every agent session reads it, so drift there degrades every session. It already drifts measurably — its skill list did not match either skill tree, and this session added tool-table rows by hand each time a tool was built. Nothing checks that what it claims exists. Split the idea in two, because the halves have very different risk. (1) VERIFICATION, safe and fully automatable: lint AGENTS.md and CLAUDE.md against the repo — every referenced script, skill, subagent, rule file and path must exist, and conversely every skill in the skill tree and every script in bin/ must be listed or deliberately excluded. Same class as validate-datasets.py: report first, --strict later. Note the vehicle: Development Principle #2 says system/repo state goes to a script or playbook and only application-record state goes to an MCP tool, so this is a bin/ tool, not an MCP server. (2) OPTIMISATION, i.e. rewriting instructions to be better rather than merely accurate: this must NOT be automated. These files are the control surface for agent behaviour, and an automated rewrite can silently weaken a hard rule in a way no test catches — the same shape as the temptation to set dev_mode true to make a check pass. Keep that half as agent-proposes/human-disposes, run as a discussion cycle like the session.log to permanent-memory cycle. Also worth adding to the automation ladder: not the accuracy check, which is not repetition-driven, but a tick each time AGENTS.md is hand-edited. Frequent hand-edits are a signal that the doc's structure is wrong or that something in it should be generated. Operator's idea 2026-08-05. | consolidated | 136 |
| 2026-08-05 | 5 | Cross-project library so collaboration tooling is inherited, not copied | Every project the operator runs needs the same collaboration-layer scaffolding: agent instruction files, skills, publication/secret guards, git hooks, a session log, an idea ledger, an MCP launcher, a definition-of-done gate. Today each project grows its own copy, and this session produced hard evidence of what that costs. (1) skills/ in this repo holds 11 skills imported from another project, including one that instructs 'npm run session:end' in a repo with no package.json and explicitly forbids the manual fallback — copied in once, never maintained, now actively misleading. (2) Two tracked skill trees here had diverged in ALL NINE shared skills, one by 19 lines. (3) MCP servers ran older copies living outside the repo for weeks (#80, #81). (4) The lifecycle-processor systemd unit executes a SIBLING repo's copy, not this one (#27). Every one of those is the same disease: a good thing copied between projects with no inheritance path, so fixes never propagate and staleness is invisible. Wants: audit the sibling projects for tools and patterns worth adopting here, push our own mature ones outward, and then decide the inheritance mechanism. Candidates for the mechanism, in rough order of preference: a git submodule or subtree of a shared 'collab-kit' consumed by each project; a versioned package installed per project; or generation from one canonical source into each consumer with a drift check like the one added in #130. Note the cautionary evidence above is all about COPYING, so whatever is chosen must make staleness LOUD — a copy that cannot tell it is stale is what produced every case listed. Operator's idea 2026-08-05. | accepted | 138 |
| 2026-08-07 | 4 | Pre-commit secret patterns are a strict subset of CI's, so local passes and CI fails | The pre-commit hook comments claim it is 'belt-and-suspenders with CI secret-scan', but the hook scans only password= and api_key= with '=', while CI scans (password\|api[_-]?key\|secret\|token) with '[:=]'. CI is strictly stricter, so a commit can pass every local gate and fail the PR — observed 2026-08-07 on a test fixture kwarg named token=. Fix: derive both from one shared pattern list (the publication-guard.sh precedent — one script, called by hook and CI), so the drift is structurally impossible rather than merely discouraged. | accepted | 157 |
| 2026-08-07 | 4 | Concurrent sessions clobber each other's .current-ticket | bin/switch-env.sh clears .current-ticket unconditionally, and it is a single shared file — so a second session switching environments silently destroys the first session's active ticket. Observed 2026-08-06: switching to another environment cleared a live ticket belonging to a parallel session, which then had no ticket for its infra commits. Same shared-mutable-state class as ACTIVE_ENV before #126/#127, and the fix is likely the same shape: let an exported variable pin the session's ticket so concurrent sessions stop colliding. | accepted | 158 |
| 2026-08-07 | 3 | A shipped MCP server is launched from a package runner instead of the in-repo copy | check-mcp-wiring.py (#146) reports one server wired to 'uvx <package>' while this repo ships its own implementation of the same server under mcp/. Two implementations, one live, and the in-repo copy is never exercised — the same copy-not-inherit class as #80/#81/#146, just via a package runner rather than a sibling checkout. Decide: retire our copy and treat the package as canonical (then remove it from the shipped-server list so the reporter stops flagging it), or cut the wiring over to bin/mcp-run.sh and keep one implementation here. | accepted | 159 |
| 2026-08-07 | 3 | Review workflows spawn subagents with unrestricted tools | A high-effort review workflow spawned 32 subagents; one probed a system credential store (/etc/shadow) with no relation to the review task. Findings still had to be re-verified by hand, so the fan-out bought coverage but also unscoped tool access. Collaboration-surface work: decide the tool allowlist a review/verify subagent actually needs (read repo files, run the repo's own tests, git/gh read) and pin it in the agent definitions, so a verifier cannot reach outside the repo at all. | accepted | 160 |
