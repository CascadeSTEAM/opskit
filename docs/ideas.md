# Idea Ledger

Raw enhancement/tooling ideas land here first, as a single ledger
row — not as an individual GitHub issue per idea. This keeps early,
overlapping ideas cheap to capture and consolidatable before they
become tracked work (Development Principle 1: never lose an idea —
but a ticket flood isn't the win condition either).

**Capture vs. triage.** Anyone (an agent or a human) can *capture* an
idea any time via `bin/idea.py add` — no judgment call required, no
board noise. *Triage* — clustering overlapping rows, deciding what's
worth a GitHub issue, and filing it — is a separate, deliberate pass,
done by the `idea-triage` skill
(`.opencode/skills/idea-triage/SKILL.md`). A row only gets a GH# once
triage has actually filed (or matched it to) an issue.

Manage this table with `bin/idea.py` (`add`, `list`, `search`,
`mark`) rather than hand-editing — it keeps pipe-escaping and row
numbering consistent. Run `bin/idea.py --help` for full usage.

| Date | Desire (1-5) | Title | Description | Status | GH# |
|------|--------------|-------|-------------|--------|-----|
| 2026-07-20 | 3 | opskit init: refuse case-insensitive duplicate env names | init scaffolded an uppercase twin of an existing environment; it should detect case-insensitive collisions and refuse (or offer to adopt the existing env) | accepted | 23 |
| 2026-07-20 | 4 | flag inventory hosts missing device YAMLs | generic check: every host in an environment inventory should have a datasets/devices YAML; scan or a lint command should report gaps | accepted | 24 |
| 2026-07-20 | 3 | run opskit lint in CI e2e job | the e2e-pipeline CI job scaffolds a fake env and scans a fixture; running opskit lint against it (and against environments/example) would catch inventory/device-YAML drift on every PR | accepted | 29 |
| 2026-07-21 | 4 | opskit scan: auto-elevate or warn when local-subnet scan runs unprivileged | on a local LAN, nmap needs root for ARP; without it the scan writes device YAMLs with empty MACs and no vendor. cmd_scan should detect a local subnet + non-root and either re-exec with sudo or emit a clear warning (nmap_runner already has use_sudo). Found during a live LAN scan. | new |  |
| 2026-07-21 | 3 | opskit scan: resolve hostnames from DNS/DHCP, not just reverse-PTR | scan named only ~14% of hosts on a live LAN; the authoritative names came from the Technitium DHCP lease table + Proxmox CT map. scan could optionally query an env-configured DNS/DHCP source (or PVE API) to name hosts instead of leaving host-<ip> stubs. | new |  |
| 2026-07-22 | 4 | System baseline capture skill | Codify the manual process of capturing a known-good system state for comparison during troubleshooting. Should capture: OS/kernel, GPU/drivers, display config (KScreen), key packages, services, network config. Output a device YAML baseline file that can be used to restage from scratch. Surfaced during a hands-on display-config debug session on a workstation. | accepted | 37 |
| 2026-07-22 | 4 | Guard branch names against client tokens | publication-guard.sh checks staged content + commit messages but NOT branch names; the AGENTS.md client-data rule explicitly forbids client-identifying branch names. Add a pre-push hook (and/or CI check) rejecting branch names that contain any .client-tokens entry. Surfaced when a client-named feature branch was found already pushed to the public origin. | new |  |
| 2026-07-23 | 3 | Weak-signal access-list pattern for CAPsMAN APs | After an AP outage, IoT clients fail over to a distant AP and stick there at unusable signal. Codify a CAPsMAN access-list (reject below ~-85 dBm) as a reusable Ansible-managed pattern so sticky clients re-home automatically. | new |  |
| 2026-07-23 | 3 | Detect duplicate DHCP client hostnames; manage reservations | Devices sharing a client hostname make the DHCP-updated DNS A record flap between them; hostname-less devices get no record. Teach scan/tooling to detect collisions and generate DHCP reservations with unique hostnames. | new |  |
| 2026-07-23 | 2 | Fix stale endsession skill (missing end-session script) | skills/endsession instructs 'npm run session:end' via scripts/end-session.ts, but neither package.json nor scripts/ exists in this repo. Port the script or rewrite the skill to match the manual AGENTS.md shutdown procedure. | new |  |
| 2026-07-25 | 4 | ACTIVE_ENV race between concurrent sessions | Two agent sessions sharing one clone fight over .env ACTIVE_ENV (observed: ticket opened with wrong env prefix after another session flipped envs mid-task). Consider per-session env override (env var beats .env), a lock, or warning when .env mtime changes mid-session. | new |  |
| 2026-07-27 | 2 | run skill: recipe for screenshotting static/browser-driven HTML pages | The 'run' skill has no built-in pattern for driving/screenshotting a plain static HTML file (no dev server, no framework) — had to install playwright locally and use channel:'chrome' to reuse the system google-chrome binary since Playwright's bundled browser build wasn't downloaded. Worth adding a short recipe/example for this shape alongside cli/server/tui/electron/playwright. | new |  |
| 2026-07-27 | 2 | Buzz booking dashboard has no way back to the main Desk | Buzz's customer-facing event-booking SPA (mounted at /dashboard) has no navigation link back to the Frappe Desk once a staff member ends up there (e.g. via an event/ticket link). Not urgent, but a real UX gap surfaced while diagnosing a live helpdesk ticket. | new |  |
| 2026-07-30 | 4 | Client helpdesk TLS cert expired - ticket creation blocked | An env helpdesk endpoint fails TLS handshake (expired proxy cert); open-ticket.sh cannot record real tickets, sessions fall back to --local. Root cause and fix path documented in that env private layer (session note 2026-07-30). Also: that env vault CF token is stale and needs rotation. | new |  |
| 2026-07-31 | 3 | Rename Frappe site to its canonical vhost to drop reverse-proxy Host rewrite | A Frappe/ERPNext site is still named after the vhost it was originally provisioned under, while the canonical public vhost is now a different name. The edge proxy compensates with a per-vhost 'header_up Host <old-site-name>' rewrite, because Frappe routes requests by Host header. Behaviour is correct, but the mismatch is permanent tech debt: the rewrite can never be removed, the old name stays load-bearing in configs/backups/bench commands, and operators querying the site must know the internal name differs from the URL (this already caused a mis-framed diagnosis in one session). Fix: bench rename-site to the canonical name, update compose/site config, backup jobs and proxy vhosts, then delete the Host-rewrite directive. Needs a maintenance window and a verified backup first. | new |  |
| 2026-07-31 | 4 | Guard: tests must not depend on presence/absence of gitignored local config | A merged change passed CI and failed locally on the byte-identical commit: a module read a gitignored local config file at import time, and the test suite inherited it. CI never has that file, so the suite was permanently green while any developer holding real config saw 41 failures. Fixed case-by-case, but the class of bug is repeatable across any tool reading a *.local.json / .env / environments overlay at import. Worth a CI job (or pytest fixture convention) that runs the suite a second time with representative local-config files present, so environment-coupled tests fail loudly in CI instead of only on developer machines. Related: any test that exec_module's a real module inherits that module's import-time side effects. | new |  |
| 2026-07-31 | 4 | Lint: quote every value interpolated into an ssh command line | A code review caught arbitrary remote command execution in a new tool: ssh appends its trailing arguments into a SINGLE string that is parsed by a shell on the remote host, so passing values as separate argv elements gives no protection whatsoever. A crafted container name turned into arbitrary execution on the target. Fixed with shlex.quote per token, but the repo builds ssh command lines in several scripts and nothing checks this. Worth a lint rule or shellcheck-style guard that flags unquoted interpolation into ssh/rsync/scp invocations, plus a short note in the relevant skill, since the mistake looks safe to reviewers who assume argv separation is honoured end to end. | new |  |
| 2026-08-02 | 4 | Guard: detect runtime MCP config pointing outside the repo | The agent runtime launched older duplicate copies of two MCP servers from a sibling repo for weeks; merged tool surface was never reachable and nothing could notice. Add a check (opskit check / CI-adjacent) that reads the runtime MCP config and flags any entry whose launch target is not this repo's bin/mcp-run.sh when an equivalent server exists in mcp/. Same class as the branch-name publication gap: the guard exists for content but not for wiring. | new |  |
| 2026-08-02 | 4 | Retire duplicated MCP server copies once the in-repo launcher is proven | Two servers still have older copies living outside the repo, wired in parallel with the new launcher. Once the new path is exercised on real work, delete the outside copies and their per-server wrapper scripts so there is exactly one implementation per server. Leaving both indefinitely reintroduces the drift the launcher was built to end. | new |  |
| 2026-08-02 | 4 | Flag environment directories with no remote in .env-remotes | An environment present locally but absent from the .env-remotes map exists on exactly one machine and is silently lost in a rebuild or workstation migration. install.sh counts environments but does not cross-check them against the remote map. Add that comparison to opskit check / install.sh preflight. | new |  |
| 2026-08-02 | 3 | Verify vault item notes against actually-granted permissions | A service-account vault item documented a permission posture that live probing contradicted (notes said roles were not granted; reads returned 200). Credential documentation drifts from reality silently and then gets trusted. A small probe tool that checks what an account can actually do, and reports drift from its documented scope, would catch this. | new |  |
| 2026-08-03 | 4 | Codify VPN peer lifecycle: create, inventory, hand-off, revoke | No process exists for managing WireGuard access. Zero playbooks/roles/skills/docs reference peer management; the private layer records subnets and a maturity:1 device stub only. Symptoms found 2026-08-03 while trying to add a user: dashboard admin password drifted from the vault (vault entry unchanged since 06-16, rejected by the live dashboard), two duplicate vault items for one credential, no inventory of who has access, no revocation path. Per Development Principles #2 this is application record state (WGDashboard API) so the vehicle is an MCP tool or codified CLI, NOT Ansible. Must cover create -> client config -> vault naming scheme -> peer inventory -> secure hand-off -> revoke. | new |  |
| 2026-08-03 | 4 | opskit lint ignores env.yml source_of_truth.format and always looks for .yml device files | bin/opskit lint reports '0/18 inventory hosts have device YAML' for an environment whose devices are stored as .md with YAML frontmatter — the format that env.yml explicitly declares via source_of_truth.format: md. So the device-coverage gate has never passed for that env and its output is pure noise, which trains operators to ignore it. Either honour source_of_truth.format, accept both extensions, or converge the layer on one format. Same class as #83: a guard whose own correctness nothing checks. Found 2026-08-03 while filling in a device record. | new |  |
| 2026-08-03 | 3 | Migrate ansible_env/ansible_hostname to ansible_facts before the injected-vars removal | ansible-core 2.21 emits a deprecation on every workstation playbook run: 'Use ansible_facts["fact_name"] (no ansible_ prefix) instead.' Both workstation playbooks use ansible_env.HOME and ansible_hostname, as do others in ansible/. Currently a warning; becomes a hard failure when injected ansible_* vars are removed. Repo-wide sweep, ansible-lint does not flag it. Noticed while fixing #84. | new |  |
| 2026-08-03 | 4 | mcp-run.sh --check reports BW_SESSION 'set' when the vault is actually locked | The check only tests that BW_SESSION is non-empty. A stale token from an auto-locked vault passes the check, then every secret resolution fails at launch — precisely the silent MCP startup failure the script exists to prevent. Observed 2026-08-03: bw status returned {"status":"locked"} while --check reported BW_SESSION OK, and bw get item hung on a hidden master-password prompt rather than erroring. Fix: run a cheap authenticated probe (bw status, assert status == unlocked) instead of a non-empty test, and surface 'locked' distinctly from 'not set'. Also worth passing </dev/null to bw calls so a locked vault errors instead of waiting on stdin. | new |  |
