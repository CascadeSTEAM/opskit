# Session Notes — 2026-08-26

## Work Done

### 1. PR #269 merged
- `6bb8bcc` — `project_sync: implement mount subcommand (Phase 2)` merged to `main`
- CI passed (1109 tests)

### 2. `bin/cleanup.sh` created
- Bash wrapper for `bin/repo-cleanup.py` with `--check` (exit 1 if work exists) and `--apply` (delete)
- Documented in AGENTS.md tool table
- Committed as `39831ae`

### 3. `/cleanup` skill trigger fix — root cause found
- **Root cause**: opencode auto-discovers slash commands from the skill's `description` field, NOT from `triggers:` frontmatter
- Skills that work as slash commands (`grind`, `idea-cmd`, `release`, `helpdesk-ticket`) all have `/command` in their description
- `cleanup` and `check-connectivity` had `/command` in triggers only — opencode never registered them
- Fixed: added `Use for: /cleanup, cleanup, ...` to cleanup description
- Also fixed: same issue in `check-connectivity` description
- Moved non-slash `cleanup` before `/cleanup` in triggers list (matches `grind` pattern)
- Synced `.claude/skills/` symlinks

### 4. Collab drift fix
- `collab_skill_drift` revealed: `plow` listed in AGENTS.md but absent on disk; `idea-cmd` on disk but not listed
- Fixed: replaced `plow` with `idea-cmd` in AGENTS.md skills list (line 135), placed alphabetically next to `idea-triage`
- `collab_verify_docs`: 41 references checked, 0 broken

### 5. Collab `bin/cleanup.sh` documentation verified
- `collab_tool_drift`: `bin/cleanup.sh` is properly documented in AGENTS.md

## Commands Run
- `python3 bin/mcp-call.py collab collab_skill_drift`
- `python3 bin/mcp-call.py collab collab_verify_docs`
- `python3 bin/mcp-call.py collab collab_tool_drift`
- `python3 bin/automation-ladder.py sync-skills`
- `make test` (1109 passed, 1 skipped)

## Git Commits (this session)
1. `37cd3c9` — opskit: fix skill list drift — replace plow with idea-cmd
2. `3c413ea` — cleanup: move non-slash 'cleanup' before '/cleanup' in triggers for opencode compatibility
3. `f02c82d` — skills: add /command patterns to descriptions so opencode auto-discovers them as slash commands

## Pushed
- Both commits pushed to origin/main
