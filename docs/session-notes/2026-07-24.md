# Session Note — 2026-07-24

## Work Done
- Installed `@smart-coders-hq/opencode-model-fallback` plugin to `~/.config/opencode/opencode.json`
- Created `~/.config/opencode/model-fallback.json` with failover chain:
  - Primary: `profile-hybrid/auto` (cluster-llm Router at `:40115`)
  - Fallback 1: `opencode/big-pickle` (BigPickle free)
  - Fallback 2: `anthropic/claude-haiku-4-5` (Claude direct)
- Created log directory: `~/.local/share/opencode/logs/`
- Added `NOTES.md` to opskit `.gitignore`

## Key Decisions
- Plugin approach chosen over manual provider config — OpenCode doesn't have native provider failover
- cluster-llm remains primary inference endpoint; never modify it directly
- Auto-recovers to cluster-llm after 5-minute cooldown

## Errors Encountered
- None

## Undo Instructions
- Remove plugin from `opencode.json`: delete `"plugin": ["@smart-coodles-hq/opencode-model-fallback"]`
- Remove fallback config: `rm ~/.config/opencode/model-fallback.json`
- Remove log dir: `rm -rf ~/.local/share/opencode/logs`

## Next Steps
- Restart OpenCode to activate plugin
- Test with `/fallback-status` command
