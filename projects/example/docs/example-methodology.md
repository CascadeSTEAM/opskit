# Example Methodology

Reference only. This stands in for the domain knowledge a real member
contributes — the material its subagent reads at runtime (listed under `docs:`
in `.opskit/pack.yml`). Keep such docs documentation-range and
environment-agnostic; real facts belong in OpsKit's gitignored
`environments/<env>/` layer, never in a member.
